<footer class="py-0.5" style="background-color: #fff; margin-top: 2rem; margin-bottom: 2rem;">
  <div class="container text-center">
    <div class="d-flex flex-column align-items-center">

      <!-- Email (copy button) -->
      <button id="copy-email-btn"
        style="background: transparent; border: none; color: #000; font-size: 0.9rem; font-weight: 700; cursor: pointer; text-transform: uppercase; text-decoration: none; font-family: 'Roboto', sans-serif;">
        ALLFORDCADEYRN@GMAIL.COM →
      </button>

      <!-- GitHub -->
      <a href="https://github.com/KAYYWHAT" target="_blank"
         style="color: #000; text-decoration: none; font-size: 0.9rem; font-weight: 700; font-family: 'Roboto', sans-serif;"
         class="footer-link">
        GITHUB →
      </a>

      <!-- Instagram -->
      <a href="https://www.instagram.com/kayywhat/" target="_blank"
         style="color: #000; text-decoration: none; font-size: 0.9rem; font-weight: 700; font-family: 'Roboto', sans-serif;"
         class="footer-link">
        INSTAGRAM →
      </a>

      <!-- LinkedIn -->
      <a href="https://www.linkedin.com/in/cadeyrnrattur" target="_blank"
         style="color: #000; text-decoration: none; font-size: 0.9rem; font-weight: 700; font-family: 'Roboto', sans-serif;"
         class="footer-link">
        LINKEDIN →
      </a>

    </div>
  </div>
</footer>

<style>
  /* Default no underline */
  .footer-link,
  #copy-email-btn {
    text-decoration: none;
  }

  /* Underline on hover/focus */
  .footer-link:hover,
  .footer-link:focus,
  #copy-email-btn:hover,
  #copy-email-btn:focus {
    text-decoration: underline;
    text-decoration-color: black;
    text-underline-offset: 3px;
    color: #000 !important;
    outline: none;
  }

  /* Remove default focus outline for keyboard users, optional */
  #copy-email-btn:focus-visible,
  .footer-link:focus-visible {
    outline: none;
  }
</style>

<script>
  document.addEventListener("turbo:load", function () {
    const button = document.getElementById("copy-email-btn");
    if (button) {
      button.addEventListener("click", () => {
        const email = "allfordcadeyrn@gmail.com";
        navigator.clipboard.writeText(email).then(() => {
          button.innerText = "COPIED!";
          button.style.color = "#155724";

          setTimeout(() => {
            button.innerText = "ALLFORDCADEYRN@GMAIL.COM →";
            button.style.color = "#000";
          }, 2000);
        }).catch(() => {
          alert("Sorry, failed to copy.");
        });
      });
    }
  });
</script>
