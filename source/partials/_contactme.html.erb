<footer class="py-0.5" style="background-color: #fff; margin-top: 2rem; margin-bottom: 2rem;">
  <div class="container text-center">
    <div class="d-flex flex-column align-items-center">

      <!-- Email -->
      <button id="copy-email-btn" type="button"
        style="background: transparent; border: none; color: #000; font-size: 0.9rem; font-weight: 700; cursor: pointer; text-transform: uppercase; font-family: 'Roboto', sans-serif;">
        ALLFORDCADEYRN@GMAIL.COM →
      </button>

      <!-- Socials -->
      <a href="https://github.com/KAYYWHAT" target="_blank" class="footer-link">GITHUB →</a>
      <a href="https://www.instagram.com/kayywhat/" target="_blank" class="footer-link">INSTAGRAM →</a>
      <a href="https://www.linkedin.com/in/cadeyrnrattur" target="_blank" class="footer-link">LINKEDIN →</a>

      <!-- Back to Top -->
      <button id="back-to-top" type="button"
        style="margin-top: 1rem; background: transparent; border: none; color: #000; font-size: 0.9rem; font-weight: 700; cursor: pointer; text-transform: uppercase; font-family: 'Roboto', sans-serif;">
        BACK TO TOP ↑
      </button>

    </div>
  </div>
</footer>

<style>
  .footer-link, #copy-email-btn, #back-to-top {
    text-decoration: none;
    color: #000;
    font-size: 0.9rem;
    font-weight: 700;
    font-family: 'Roboto', sans-serif;
    margin: 0.2rem 0;
  }

  .footer-link:hover, .footer-link:focus,
  #copy-email-btn:hover, #copy-email-btn:focus,
  #back-to-top:hover, #back-to-top:focus {
    text-decoration: underline;
    text-underline-offset: 3px;
    color: #000 !important;
  }
</style>

<script defer>
  document.addEventListener("DOMContentLoaded", function () {
    // Copy email
    const emailBtn = document.getElementById("copy-email-btn");
    if (emailBtn) {
      emailBtn.addEventListener("click", () => {
        navigator.clipboard.writeText("allfordcadeyrn@gmail.com")
          .then(() => {
            emailBtn.innerText = "COPIED!";
            emailBtn.style.color = "#155724";
            setTimeout(() => {
              emailBtn.innerText = "ALLFORDCADEYRN@GMAIL.COM →";
              emailBtn.style.color = "#000";
            }, 2000);
          })
          .catch(() => alert("Failed to copy"));
      });
    }

    // Back to top
    const backBtn = document.getElementById("back-to-top");
    if (backBtn) {
      backBtn.addEventListener("click", () => {
        // This always works even if Turbo or scroll container exists
        window.scrollTo({ top: 0, left: 0, behavior: 'smooth' });
      });
    }
  });
</script>
